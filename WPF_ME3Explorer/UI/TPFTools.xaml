<Window x:Class="WPF_ME3Explorer.UI.TPFTools"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_ME3Explorer.UI"
        mc:Ignorable="d"
        Title="TPFTools" Height="466.802" Width="974.27">
    <Grid Margin="0,0,0.333,31.333">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="642*"/>
            <ColumnDefinition Width="325*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="27*"/>
            <RowDefinition Height="136*"/>
            <RowDefinition Height="167*"/>
            <RowDefinition Height="77*"/>
        </Grid.RowDefinitions>

        <Menu Grid.ColumnSpan="2" Background="White" Margin="0,0,0.333,0.667">
            <MenuItem x:Name="LoadButton" Header="Load"/>
            <MenuItem x:Name="ClearAllButton" Header="Clear All Textures"/>
            <MenuItem x:Name="AutofixAllButton" Header="Autofix All Invalids"/>
            
        </Menu>
        <ListBox x:Name="MainListBox" Grid.RowSpan="2" Grid.Row="1"/>
    </Grid>
</Window>
